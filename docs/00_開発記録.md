# flood_pso 開発記録

> 起点：2026-04-28
> 目的：`kennkyuu20260114/` の研究軸（**PSO改善手法 = CCPSO2** をメイン、応用例として高次元最適化）と `flood_pso/` 実装を結合し、御坊市・日高川の洪水校正問題で **標準PSO vs CCPSO2** を実証する。
>
> 本ファイルには、すべての実装ステップ・意思決定・実行結果を時系列で記録する。
> 個別トピックの詳細は `docs/` 配下の番号付きMarkdownに分割する。

---

## 研究軸との対応関係

| 軸（kennkyuu20260114） | 対応する flood_pso 実装 |
|---|---|
| メイン：PSO改善手法（CCPSO2）の有効性を高次元問題で示す | `src/ccpso2.py`、`src/benchmark.py` |
| サブ：実問題への応用 | 御坊市DEMによる洪水校正（既存 + HD化） |
| 応用例論文の★★★：施設配置最適化（高次元） | DEMをK×K分割し粗度マップを高次元最適化 |
| **実装系の研究**: 結果を再現可能な NBT 形式でアーカイブ | `src/nbt_export.py` (+meta), `src/make_nbt_hd.py` |
| 来年度本筋：IA（免疫アルゴリズム）応用 | CCPSO2の分割統治知見を将来IAに展開（記録のみ） |

---

## 進行ステップ（チェックリスト）

- [x] **Phase 0-1**: 開発記録ドキュメントの基盤作成（このファイル）
- [x] **Phase 0-2**: Linux環境でmain.pyを実行可能に — `01_環境構築.md`
- [x] **Phase 0-3**: ベースライン（2変数PSO）再現 — `02_ベースライン再現.md`（IoU=0.9929）
- [x] **Phase 1**: 高次元化問題の設計と実装 — `03_高次元化設計.md`、`flood_sim.py:simulate_flood_hd`、`pso_calibrate_hd.py`
- [x] **Phase 2**: CCPSO2 実装 — `04_CCPSO2実装.md`、`src/ccpso2.py`
- [x] **Phase 3**: 標準PSO vs CCPSO2 ベンチマーク — `05_ベンチマーク結果.md`
- [x] **Phase 4**: NBT 管理（実装系研究軸への拡張）— `06_NBT管理.md`、`src/nbt_export.py`、`src/make_nbt_hd.py`
- [x] **Phase 5**: ネイティブ GPU ビューアの別リポジトリ化（2026-05-01〜07）— **`flood_pso_viewer/`**
- [x] **Phase 6**: 地形レンダ改善（Tellus 風 enhanced モード）— `07_地形レンダ改善.md`、`src/terrain_render.py`
- [x] **Phase 7**: Tellus データソース直接取得 + Mapzen 上書き（`07_地形レンダ改善.md §7`、`src/tellus_data.py`）
- [x] **Phase 8**: 歩行可能な高精細ワールド（FG-GML 建物・道路の配線 + 真スケール preset）— `src/fgd_vector.py`、`nbt_export.py:export_to_nbt(use_fgd=...)`、`make_nbt_hd.py` の `gobo_walk_*` preset / `--use-fgd`

## Phase 5：ネイティブ GPU ビューア（外部リポジトリ）

NBT を Web で見ようとするとブラウザがクラッシュするため、Rust + Bevy 0.15 でネイティブビューアを別リポジトリとして実装。

- リポジトリ: <https://github.com/ntaku256/flood_pso_viewer>
- ローカル: `/home/moriken/web-app/flood_pso_viewer/`
- 仕様詳細: `flood_pso_viewer/docs/00_開発記録.md`

主要機能：
- `flood_pso_meta` コンパウンドを右パネルに展開（method / K / D / loss / IoU / dh_map ヒートマップ）
- Minecraft Creative 風 FlyCam（WASD + Tab マウス capture + Space/Shift 上下）と Orbit カメラを F で切替
- `--no-water` または V キーで **浸水前 ↔ 浸水後** をワンクリック比較
- WSL2 の Mesa Zink 経由でホスト GPU（AMD Radeon 等）が掴めるようバックエンド優先順位を自動判定
- 自作 `VoxelMaterial`（埋め込み WGSL）で Bevy 標準 PBR の WSL 互換性問題を回避
- XZ チャンク分割 greedy meshing（rayon 並列）で frustum culling を効かせる

参考実装：
- `web-app/schematic-renderer/mesh_builder_wasm/src/lib.rs` — greedy meshing アルゴリズム
- `web-app/redstone-lib/packages/viewer` — 軽量 isometric ビューア構成

## Phase 6：地形レンダ改善（Tellus 風 enhanced モード）

`flood_pso_viewer` で見たときの地形「精度の低さ」3 点（海岸線・海の表現が雑、緑斜面の階段化、地表ブロックの不自然さ）を、`web-app/Tellus/`（Earth-scale Minecraft Fabric mod）の worldgen 実装を参考に改善。
詳細は `docs/07_地形レンダ改善.md`。

主要変更：
- 新ファイル `src/terrain_render.py` — cliff-aware smoothing、海岸距離→段階水深、slope/convexity ベースの地表ブロック分類、陸/海で v_exag 分離
- `nbt_export.py:export_to_nbt(terrain_quality="enhanced"|"legacy", ...)` — 切替フラグ追加、enhanced を default に
- `make_nbt.py` / `make_nbt_hd.py` に `--quality {enhanced,legacy}` / `--sea-level` 追加
- legacy 出力はファイル名に `_legacy` サフィックス、enhanced（無印）と並存可能
- 研究本体（PSO/CCPSO2 校正）には影響しない（`simulate_flood_hd` 出力をそのまま使う、ブロック化のレンダ層だけ変更）

## Phase 8：歩行可能な高精細ワールド（2026-06-17）

目的：研究軸（PSO/CCPSO2 校正）とは別の **Layer C / 防災教育コンテンツ軸**。
「マイクラで実際に歩き回れる」精度（1 ブロックあたりの m を小さく）を目指す。

ボトルネック整理：
1. 元データが GSI 5m（DEM5A/5B）。真の 5m 以下は **DEM1A(1m LiDAR)** 入手か補間が必要。
   `dem_parser.py` は GML ヘッダから grid 寸法を読む汎用実装なので **DEM1A はコード変更ゼロで読める**
   （`FLOOD_PSO_DEM_DIR` を DEM1A ディレクトリへ向けるだけ）。DEM1A は FGD ダウンロード
   サービス（要ログイン）からのみ。和歌山の整備可否は 2025-03 拡大リストに明記なし→要確認。
2. 1m/block 自体は **enhanced 経路が既に bilinear アップサンプル**（`nbt_export.py` の
   `if h_res < h_res_dem*0.95: nd_zoom`）。5m DEM でも 1m/block は出る（滑らかだが真の 1m 詳細は無い）。
3. 歩ける町には建物・道路が必須。OSM 経路（`--use-osm`）はあったが、**FG-GML ローカルベクタ
   （BldA/RdEdg、API 不要・高精度）が未配線**だった。

実装：
- `src/fgd_vector.py` 新設。BldA(建物ポリゴン)・RdEdg(道路ライン)を逐次パースし、
  `tellus_data.fetch_osm_buildings_roads` と同じ dict 形に変換（bbox クリップ・LFS 実体化要）。
  → 下流のラスタライズ（`terrain_render.build_osm_masks`）と配置（`dem_to_blocks_enhanced`）を
    一切変更せず再利用。
- `nbt_export.export_to_nbt(use_fgd, fgd_bld_xml, fgd_rdedg_xml)` 追加。OSM と併用時は mask を union。
- `make_nbt_hd.py`：`--use-fgd` と歩行用 preset `gobo_walk_1km/2km`（1m/block, **v_exag=1 真スケール**,
  市街地中心 33.8875,135.1515）を追加。出力名サフィックス `_fgd`。

実測（gobo_walk_1km, 500m×500m, gt）：建物 815 棟・道路 96 本を配置（building mask 85,863 / road 61,668 セル）、
598×15×498 ブロック、5.1MB、31s。トップダウンで日高川・建物群・道路網が視認でき「町」になった。

### 真の 1m 地形：和歌山県 LiDAR（2026-06-17 続き）

GSI DEM1A は御坊で未整備の公算大（2025-03 拡大に和歌山なし）。代わりに **和歌山県が独自公開する
航空レーザ測量点群オープンデータ**（2023-03〜, 県域65%, 御坊含む）を採用。
- ポータル `https://wakayamaken.geocloud.jp/mp/22`（手動DL）。図郭 `06RC802` を取得（grd 133MB / org 388MB）。
- 形式：CSV `id, easting, northing, Z`（**平面直角座標系VI = EPSG:6674, JGD2011**, CRLF）。
  grd=グラウンド(地形), org=オリジナル(DSM)。06RC802 は lat[33.874,33.888] lon[135.157,135.178] の 2km×1.5km。
- 新ファイル `src/wakayama_pcd.py`：点群 370万点 → pyproj で系VI→WGS84 → 1m 緯度経度グリッドに bincount 集約
  → NaN 隙間を最近傍埋め → `dem_parser` 互換 dict。.npz キャッシュ（2 回目以降 2 秒）。要 `pip install pyproj pandas`。
- `make_nbt_hd.py --wakayama-grd <grd.txt>`：DEM ソースをこれに切替、中心はタイル被覆へ自動設定。
  h_res=1m かつ DEM が 1m なので **アップサンプル無し＝ネイティブ真の 1m**。
- 検証：gobo_walk_1km gt lidar → 500×57×498（起伏が 57 ブロックに反映）, 191 棟, 5.9MB, 32s。

### 地表色を空中写真から（写真駆動の地表, 2026-06-17 続き）

地表ブロックを傾斜分類でなく **GSI シームレス空中写真（seamlessphoto, z18≈0.6m/px）** から決める。
- `tellus_data.fetch_gsi_ortho`：seamlessphoto タイル取得 → RGB モザイク（`fetch_mapzen_dem` 踏襲）。
- `src/ortho_surface.py` 新設：`classify_rgb_to_palette` で RGB→**viewer 既知のバニラブロック**
  （grass/sand/gravel/stone/water/bedrock）へヒューリスティック分類。`ortho_surface_grid` が
  reproject_to_grid で patch 格子へ整列。NBT は Minecraft 互換、viewer 再ビルド不要。
- `nbt_export.export_to_nbt(surface_ortho, ortho_zoom)`：既存の `surface_grid_override`（最優先の
  地表色差し込み口）に流す。`make_nbt_hd.py --surface-ortho/--ortho-zoom`、出力名 `_ortho`。

較正の肝：御坊オルソは青み・低彩度のかぶりがあり exg が弱い（中央値~7）。最初 water 54%/grass 1% と
誤分類 → 「弱い緑も植生(grass)」を既定にし、非緑を明度で建物(stone)/道路(gravel)/裸地(sand)へ、
water は強い青(b>r+35 & b>g+25)のみ採用。結果 grass89%/gravel5.5%/stone3.8%/sand1.4%/water0% と妥当化。
（`results/nbt_preview/_ortho_compare_500m.png` で実画像と並べて較正）

### 地表マッピングのリッチ化（~80 バニラブロック, 2026-06-17 続き）

6色ヒューリスティックを、**~80 単色バニラブロックへの最近傍カラーマッチ**に拡張（写真モザイク化）。
- `src/block_palette.py` 新設＝**色テーブルの単一真実源**（concrete16/terracotta16/wool16/自然/木/葉、計89キー）。
  ここから (a) `nbt_export.PALETTE`、(b) `ortho_surface` のマッチアンカー、(c) `nbt_preview` の色、
  (d) **viewer 用 `block_colors.rs`**（`export-viewer` で自動生成）を全部供給。
- `ortho_surface.classify_rgb_to_palette` を RGB ユークリッド最近傍マッチに置換（~80アンカー、ベクトル化）。
- **flood_pso_viewer をデータ駆動化**：`voxel.rs` の `Material` を enum→「palette index newtype + per-load
  `Palette`(色/透過テーブル)」へ。`nbt_loader` が NBT palette 名から `block_colors.rs` で色解決、
  `greedy_mesh`/`render` は palette 経由。未知ブロックはマゼンタ。→ 任意のバニラブロックを正色で描画。
  （8マテリアル固定enum時代の magenta 問題を解消）。再ビルド ~44s。
- 検証：gobo_walk_1km gt + 和歌山LiDAR + ortho rich → 地表に25種超のブロック、viewer で 284 entities
  正色描画（マゼンタ無）。

### 発色補正（白黒っぽさ対策）＋ 建物屋根のマッピング（2026-06-17 続き）

マップ mod で見ると「色あせた白黒衛星画像」っぽい指摘 → オルソの霞・青みかぶり・低彩度が原因。
- `ortho_surface.enhance_rgb`：**チャンネル別パーセンタイル・ストレッチ（青みかぶり除去＋コントラスト＝発色）
  ＋彩度**。御坊中心など陸の多いタイルで鮮やか。海沿い(06SC002)は赤chを過増幅しピンク化する弱点が
  あったため、**各chの伸長倍率を中央値比 scale_cap でクランプ**（弱いchだけ抑制、均等タイルはフル vivid）。
  ※gray-world に一度替えたら発色が落ちたため、cap 付き per-channel に戻した。SC002(海主体)はなお過飽和
  気味＝側タイルとして許容（陸タイルの vivid を優先）。`--ortho-saturation` 既定1.4。
  → マッチ結果が鮮やかなカラー航空写真調に（緑/茶/白の自然な分布、60+ ブロック使用）。
  `--ortho-saturation`（既定1.4, 2+で過飽和）。
- **建物屋根のオルソ色化**：`dem_to_blocks_enhanced(color_building_roofs=)` 追加。建物トップブロックを
  `surf_block`（オルソ由来の屋根色）に、壁は stone のまま。`surface_ortho` 有効時に自動 ON。
  → 上空 mod ビューで屋根が実写真色に。

### 建物の高精度化：DSM 実高さ＋壁＋窓＋オルソ屋根（2026-06-17 続き）

「建物をそれっぽく・側面マッピング・構造精度」要望に対応。
- **PLATEAU は御坊未整備**（CKAN 30205 に3D都市モデル無し）→ 真の壁テクスチャ(LOD2)は不可。
- **構造高さ/屋根形状＝和歌山 LiDAR DSM（`_org`）**：DSM−DEM=物体高（地面0.45m中央, 建物6〜15m）。
  `make_nbt_hd` が `_grd`→`_org` を読み reproject、`building_height_grid` を `export_to_nbt`→
  `dem_to_blocks_enhanced(building_height_patch)` へ。建物 footprint セルを**実測高さ＋per-cell屋根形状**で建てる。
  `--no-building-heights` で一律高さに戻せる。
- **側面（壁）＝合成**（壁の実写真は無い）：`dem_to_blocks_enhanced(wall_block="white_concrete",
  window_block="gray_concrete", floor_height=3)`。明るい壁＋**階ごとの窓帯**＋屋根トップはオルソ色。
  → マップ mod の斜め視点で建物らしく見える。検証：建物カラムが 石基礎→白壁→3階ごと gray 窓帯→オルソ屋根、
  屋根は79種のオルソ色で変化。
- 注：洪水(GT)が建物に被ると最上が water になるが `--no-water` で屋根が出る。

未了：
- 壁は一律白＋固定窓。per-building 壁色のばらつき・樹木の3D化（DSMの非建物高さ）は将来。
- LiDAR タイルは市街地東側中心（密集市街地は西端寄り）。複数図郭の連結や中心調整は要なら対応。
- ビン化で 1m グリッドの ~40% が隙間→最近傍埋め（実効 ~1.4m）。res 調整 or org(DSM) 併用は将来課題。
- 建物高さは現状 `building_height_m` 一律（per-type 高さは `fgd_vector` が tags に持つが配置側は未使用）。
- 建物屋根は stone 一律（オルソ屋根色化は小改修で可）。viewer の豊かなパレットは voxel.rs 拡張＋再ビルドが必要。
- オルソ地表は salt-and-pepper ノイズあり（3×3 多数決フィルタで平滑化可）。

## 主要結果（要約）

| K | D | PSO_loss (depth MAE) | CCPSO2_loss | 優位 |
|---|---|---|---|---|
| 4 | 17 | 0.0007 ± 0.0005 | 0.0145 ± 0.0063 | **PSO** |
| 8 | 65 | 0.0135 ± 0.0027 | 0.0649 ± 0.0138 | **PSO** |
| 16 | 257 | 0.1629 ± 0.0422 | **0.1148 ± 0.0163** | **CCPSO2** |

→ **D=257（高次元）で CCPSO2 が標準PSO を逆転。分散も小さく安定**。研究仮説（CCPSO2 は高次元問題に有効）の実証エビデンス。

---

## ログ

### 2026-04-28 - Phase 0-1：記録基盤の整備

- `flood_pso/docs/` 作成
- `docs/00_開発記録.md`（本ファイル）を起点として全実装を時系列で残す
- 詳細トピックは番号付きで分割：
  - `01_環境構築.md` ：Linux環境構築・パス修正
  - `02_ベースライン再現.md` ：2変数PSO実行結果
  - `03_高次元化設計.md` ：粗度マップ空間分布化の問題設計
  - `04_CCPSO2実装.md` ：CCPSO2のアルゴリズム実装メモ
  - `05_ベンチマーク結果.md` ：両手法の比較結果
  - `06_NBT管理.md` ：NBT 化・メタデータスキーマ・大規模プリセット

